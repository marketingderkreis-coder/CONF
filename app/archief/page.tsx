import {PageHead} from '@/components/ui'; import {items} from '@/lib/demo';
export default function Page(){return <><PageHead title="Archief" subtitle="Volledige historie met resultaten, uren, bestanden en audittrail"/><div className="mb-4 flex gap-3"><input className="field max-w-md" placeholder="Doorzoek het archief…"/><select className="field w-48"><option>Alle jaren</option><option>2026</option></select></div><div className="card p-8 text-center"><p className="text-sm text-slate-500">Gearchiveerde items worden hier onveranderbaar en volledig doorzoekbaar bewaard.</p><div className="mx-auto mt-6 max-w-2xl divide-y text-left">{items.slice(0,3).map(i=><div className="flex justify-between py-4" key={i.id}><div><b className="text-sm">{i.title}</b><p className="text-xs text-slate-500">{i.type} · {i.profiles[0]}</p></div><span className="pill bg-slate-100">Afgerond</span></div>)}</div></div></>}

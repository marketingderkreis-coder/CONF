import {PageHead,QueryMessage} from '@/components/ui'; import {getMarketingItems} from '@/lib/data';
export const dynamic='force-dynamic';
export default async function Page(){const {data:items,error}=await getMarketingItems('archive');return <><PageHead title="Archief" subtitle="Volledige historie met resultaten, uren, bestanden en audittrail"/><div className="mb-4 flex gap-3"><input className="field max-w-md" placeholder="Doorzoek het archief…"/><select className="field w-48"><option>Alle jaren</option></select></div>{error||!items.length?<QueryMessage error={error} empty="Het archief is leeg."/>:<div className="card p-8"><div className="mx-auto max-w-2xl divide-y text-left">{items.map(i=><div className="flex justify-between py-4" key={i.id}><div><b className="text-sm">{i.title}</b><p className="text-xs text-slate-500">{i.type?.name??'Geen type'}</p></div><span className="pill bg-slate-100">{i.status}</span></div>)}</div></div>}</>}

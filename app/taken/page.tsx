import {PageHead,QueryMessage} from '@/components/ui'; import {TaskTable} from '@/components/task-table'; import {getTasks} from '@/lib/data';
export const dynamic='force-dynamic';
export default async function Page(){const {data,error}=await getTasks();return <><PageHead title="Taken" subtitle="Plan, verdeel en volg alle marketingwerkzaamheden"><button className="btn">Nieuwe taak</button></PageHead><div className="mb-4 flex flex-wrap gap-2"><input className="field max-w-xs" placeholder="Zoek een taak…"/><select className="field w-44"><option>Alle statussen</option></select><select className="field w-44"><option>Alle medewerkers</option></select><select className="field w-44"><option>Alle kanalen</option></select></div>{error?<QueryMessage error={error}/>:<TaskTable rows={data}/>}</>}

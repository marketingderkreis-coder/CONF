'use client'; import {useState} from 'react'; import {createClient,isConfigured} from '@/lib/supabase';
export default function Login(){const [msg,setMsg]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const sb=createClient();if(!sb){setMsg('Configureer Supabase om in te loggen.');return}const {error}=await sb.auth.signInWithPassword({email:String(f.get('email')),password:String(f.get('password'))});setMsg(error?.message||'Ingelogd.')}return <div className="mx-auto mt-20 max-w-md card p-8"><div className="mb-8 text-center"><div className="mx-auto mb-3 grid size-12 place-items-center rounded-xl bg-brand-600 font-bold text-white">DK</div><h1 className="text-xl font-bold">Welkom bij Marketing Hub</h1><p className="text-sm text-slate-500">Log in met je DER KREIS-account</p></div>{!isConfigured&&<div className="mb-5 rounded-lg bg-amber-50 p-3 text-xs text-amber-800">Supabase-credentials ontbreken.</div>}<form onSubmit={submit} className="space-y-4"><label><span className="label">E-mailadres</span><input name="email" type="email" required className="field"/></label><label><span className="label">Wachtwoord</span><input name="password" type="password" required className="field"/></label><button className="btn w-full">Inloggen</button></form><button className="mt-4 w-full text-center text-sm text-brand-600">Wachtwoord vergeten?</button>{msg&&<p className="mt-4 text-center text-xs text-slate-500">{msg}</p>}</div>}

import {items} from '@/lib/demo'; import {PageHead,Status} from '@/components/ui'; import Link from 'next/link'; import {Filter,Plus} from 'lucide-react';
export default function Page(){return <><PageHead title="Marketingitems" subtitle="Alle centrale tickets, campagnes en marketingmomenten"><div className="flex gap-2"><button className="btn-secondary"><Filter size={16}/>Filters</button><Link className="btn" href="/marketingitems/nieuw"><Plus size={16}/>Nieuw item</Link></div></PageHead><div className="card overflow-hidden">{items.map(i=><Link href={'/marketingitems/'+i.id} key={i.id} className="grid gap-3 border-b p-5 last:border-0 hover:bg-slate-50 md:grid-cols-[1fr_170px_120px_150px]"><div><div className="mb-1 flex items-center gap-2"><b>{i.title}</b>{i.priority==='Urgent'&&<span className="pill bg-red-50 text-red-700">Urgent</span>}</div><p className="text-sm text-slate-500">{i.summary}</p><div className="mt-3 flex gap-1">{i.profiles.map(p=><span className="pill bg-slate-100 text-slate-600" key={p}>{p}</span>)}</div></div><div><p className="text-xs text-slate-400">Type</p><p className="mt-1 text-sm">{i.type}</p></div><div><Status>{i.status}</Status></div><div><p className="text-xs text-slate-400">Voortgang · {i.progress}%</p><div className="mt-2 h-2 rounded-full bg-slate-100"><div className="h-full rounded-full bg-brand-500" style={{width:i.progress+'%'}}/></div></div></Link>)}</div></>}

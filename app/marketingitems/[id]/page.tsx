import {items,tasks} from '@/lib/demo'; import {notFound} from 'next/navigation'; import {PageHead,Status} from '@/components/ui'; import {TaskTable} from '@/components/task-table';
export default function Page({params}:{params:{id:string}}){const item=items.find(i=>i.id===params.id);if(!item)notFound();return <><PageHead title={item.title} subtitle={`${item.type} · Eigenaar Pieter de Vries`}><div className="flex gap-2"><button className="btn-secondary">Bewerken</button><button className="btn">Taak toevoegen</button></div></PageHead><div className="grid gap-5 lg:grid-cols-[1fr_300px]"><div><section className="card mb-5 p-6"><p className="text-xs font-bold uppercase tracking-wide text-brand-600">Teamsamenvatting</p><p className="mt-2 text-lg leading-relaxed">{item.summary}</p></section><h2 className="mb-3 font-bold">Taken</h2><TaskTable rows={tasks.filter(t=>t.item===item.title)}/></div><aside className="card h-fit p-5"><div className="space-y-5 text-sm"><div><p className="label">Status</p><Status>{item.status}</Status></div><div><p className="label">Prioriteit</p><b>{item.priority}</b></div><div><p className="label">Publicatiedatum</p><b>{item.date}</b></div><div><p className="label">Voortgang</p><b>{item.progress}%</b><div className="mt-2 h-2 rounded bg-slate-100"><div className="h-full rounded bg-brand-500" style={{width:item.progress+'%'}}/></div></div><div><p className="label">Communicatieprofielen</p>{item.profiles.map(p=><span className="pill mr-1 mt-1 bg-slate-100" key={p}>{p}</span>)}</div><div><p className="label">Activiteit</p><p className="text-xs text-slate-500">14 sep 09:31 · Pieter maakte het item aan</p><p className="mt-2 text-xs text-slate-500">14 sep 10:05 · Taak toegewezen aan Sanne</p></div></div></aside></div></>}

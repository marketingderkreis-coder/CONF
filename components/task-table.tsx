import type {Task} from '@/lib/data'; import {Avatar,QueryMessage,Status} from './ui'; import {Calendar,Ellipsis} from 'lucide-react';
export function TaskTable({rows}:{rows:Task[]}){if(!rows.length)return <QueryMessage empty="Er zijn nog geen taken."/>;return <div className="card overflow-x-auto"><table className="w-full min-w-[820px] text-left text-sm"><thead className="border-b bg-slate-50/70 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-3">Taak</th><th>Status</th><th>Verantwoordelijke</th><th>Planning</th><th>Werklast</th><th/></tr></thead><tbody>{rows.map(t=>{const owner=t.assignee?.full_name??'Niet toegewezen';return <tr className="border-b last:border-0 hover:bg-slate-50" key={t.id}><td className="px-5 py-4"><b className="font-semibold">{t.title}</b><p className="mt-1 text-xs text-slate-500">{t.marketing_item?.title??'Losse taak'} · {t.channel?.name??t.category??'Geen kanaal'}</p></td><td><Status>{t.status}</Status></td><td><div className="flex items-center gap-2"><Avatar name={owner}/>{owner}</div></td><td><span className="flex items-center gap-1.5 text-slate-600"><Calendar size={14}/>{t.planned_date||t.deadline||'Backlog'}</span></td><td>{t.workload}</td><td><button className="p-2"><Ellipsis size={18}/></button></td></tr>})}</tbody></table></div>}
